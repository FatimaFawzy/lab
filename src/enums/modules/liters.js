import { EnumTrait } from "@/enums/modules/EnumTrait";

export const liters = Object.freeze({
     ...EnumTrait,
     50: {
          value: 50,
          label: "50",
     },
     100: {
          value: 100,
          label: "100",
     },
});
